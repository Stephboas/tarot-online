// MenuHeaderLoggedIn.js
import React from "react";

const MenuHeaderLoggedIn = ({ userData }) => {
  return (
    // Sua implementação para o cabeçalho do menu quando o usuário está logado
  );
};

export default MenuHeaderLoggedIn;
